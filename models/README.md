This folder stores the trained LightGBM model for the stock signal dashboard.

Steps:
1. Create training data in `data/sample_training_data.csv`.
2. Run `python train_model.py`.
3. The model will save to `models/lightgbm_stock_signal.pkl`.
